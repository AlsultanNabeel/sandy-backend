package com.sandy.app.ui.daily

import androidx.compose.runtime.Composable
import com.sandy.app.ui.components.TabPlaceholder

/** Daily tab — planning (tasks + reminders + habits + focus). Ported later. */
@Composable
fun DailyScreen() = TabPlaceholder("daily.title")
