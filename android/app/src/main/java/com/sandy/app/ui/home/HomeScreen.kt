package com.sandy.app.ui.home

import androidx.compose.runtime.Composable
import com.sandy.app.ui.components.TabPlaceholder

/** Home tab — Sandy's live entry + dashboard. Content ported in a later pass. */
@Composable
fun HomeScreen() = TabPlaceholder("home.title")
