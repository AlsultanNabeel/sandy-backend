# Sandy camera — two 1.9 MB app slots so it can update itself from the server
# (sandy_ota_pull.h), plus a slot for crash dumps. Arduino picks this file up
# automatically because it sits next to the sketch. Same NVS offset as before,
# so the saved Wi-Fi, identity and settings survive the first cable flash.
# Name,    Type, SubType,  Offset,   Size
nvs,       data, nvs,      0x9000,   0x5000
otadata,   data, ota,      0xe000,   0x2000
app0,      app,  ota_0,    0x10000,  0x1E0000
app1,      app,  ota_1,    0x1F0000, 0x1E0000
spiffs,    data, spiffs,   0x3D0000, 0x20000
coredump,  data, coredump, 0x3F0000, 0x10000
